# Django
SECRET_KEY=u9$k8!zQ2#pL7@vR5^mN3xT6&cD1!sW8yF4hJ0
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# Options: postgresql | sqlite
DB_ENGINE=sqlite
DB_NAME=db.sqlite3

# PostgreSQL settings (used when DB_ENGINE=postgresql)
# DB_NAME=ems_db
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432

# CORS (comma-separated origins for React frontend)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email — use smtp backend to send real emails (console only prints to terminal)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=leavedesk24@gmail.com
EMAIL_HOST_PASSWORD=aixg dgwx opui mquj
DEFAULT_FROM_EMAIL=leavedesk24@gmail.com

# Frontend URL (used in email templates)
FRONTEND_URL=http://localhost:3000
